using System;
using System.Drawing;

namespace KitBox
{
    public class GlassDoor : Door
    {
        public GlassDoor(decimal price, int width, int height, int depth, Color color) : base(price, width, height, depth, color)
        {
        }
    }
}
